import React from 'react';
import { shallow } from 'enzyme';
import { assert } from 'meteor/practicalmeteor:chai';

import {{ properCase name }} from '../index.jsx';

describe('<{{ properCase name }} />', () => {
  it('should render', () => {
    const wrapper = shallow(<{{ properCase name }} />);

    assert(wrapper);
  });
});
